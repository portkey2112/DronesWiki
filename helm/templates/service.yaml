apiVersion: v1
kind: Service
metadata:
  name: {{ include "drone-chart.fullname" . }}
  labels:
    {{- include "drone-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "drone-chart.selectorLabels" . | nindent 4 }}
